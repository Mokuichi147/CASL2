P183Q3	START
	RPUSH
;
	LAD	GR1,3
	LAD	GR2,10
	CALL	MULT
	ADDA	GR1,=5
	CALL	MULT
;
	RPOP
	RET
;
MULT	PUSH	0,GR2
	PUSH	0,GR3
	LD	GR3,GR1
	LAD	GR1,0
LOOP	CPA	GR2,=0
	JZE	FIN
	ADDA	GR1,GR3
	LAD	GR2,-1,GR2
	JUMP	LOOP
FIN	POP	GR3
	POP	GR2
	RET
;
	END